homebrew_uninstalled_packages:
  []
  # - osxfuse
  # - sshfs
  # - lastpass-cli
  # - bash-completion
  # - pdftohtml
  # - mytop
homebrew_cask_uninstalled_apps:
  []
  # - nextcloud
  # - lastpass
  # - sublime-text
  # - skype-for-business
  # - fedora-media-writer
  # - dosbox
homebrew_cask_apps:
  - 1password  # Essential for Phase 1 bootstrap (secrets management)
homebrew_use_brewfile: true
homebrew_clear_cache: true
homebrew_upgrade_all_packages: true
homebrew_cask_accept_external_apps: true
homebrew_group: "staff"
homebrew_user: "{{ ansible_user }}"
homebrew_cask_appdir: /Applications
homebrew_installed_packages:
  - bash  # Modern bash (macOS ships with old version)
  - git   # Essential for Phase 1 bootstrap
  - jq
  - node  # Required for npm_packages (claude-code)
  - pyenv  # Python version management (needed for Phase 3)
  - pyenv-virtualenv  # Python virtualenv management (needed for Phase 3)
  - 1password-cli  # 1password command line tool (needed for Phase 3)
  - uv  # Fast Python package installer and tool manager
homebrew_taps: []
mas_installed_apps: []
